diff -up xrandr-1.3.4/xrandr.c.jx xrandr-1.3.4/xrandr.c
--- xrandr-1.3.4/xrandr.c.jx	2010-10-12 18:13:37.000000000 -0400
+++ xrandr-1.3.4/xrandr.c	2011-10-03 14:03:53.352240875 -0400
@@ -966,10 +966,8 @@ set_gamma_info(output_t *output)
 	return;
 
     size = XRRGetCrtcGammaSize(dpy, output->crtc_info->crtc.xid);
-    if (!size) {
-	warning("Failed to get size of gamma for output %s\n", output->output.string);
+    if (!size)
 	return;
-    }
 
     gamma = XRRGetCrtcGamma(dpy, output->crtc_info->crtc.xid);
     if (!gamma) {
